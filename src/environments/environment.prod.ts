export const environment = {
  production: true,
  /** Placeholder until Phase 5 assigns a real EC2 Elastic IP — edit and rebuild the image when it does. */
  apiBaseUrl: 'http://REPLACE_WITH_EC2_IP:8080/api',
};
